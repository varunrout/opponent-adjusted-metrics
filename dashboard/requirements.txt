# Dashboard Requirements
# These are now also declared in pyproject.toml under [tool.poetry.group.dashboard].
# Install via: poetry install --with dashboard
# This file is kept for standalone deployment (e.g., Docker, Streamlit Cloud).

streamlit>=1.28.0
mplsoccer>=1.2.0
plotly>=5.18.0
matplotlib>=3.8.0
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0  # For parquet support
