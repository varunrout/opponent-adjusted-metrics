############################################
# Environment Example for Local Development #
############################################

# ----------------------------------------------------------------------------
# PostgreSQL (Docker Compose)
# ----------------------------------------------------------------------------
# These are used by docker-compose.yml to provision Postgres. You can override
# them in your real .env if needed.
POSTGRES_USER=app
POSTGRES_PASSWORD=app
POSTGRES_DB=opponent_metrics

# ----------------------------------------------------------------------------
# Database Backend & URL
# ----------------------------------------------------------------------------
# DATABASE_BACKEND controls which engine is used by the app:
#   - postgres (default): use DATABASE_URL as-is
#   - sqlite: override DATABASE_URL and use data/opponent_adjusted.db
#
# In most cases you only need to toggle DATABASE_BACKEND; DATABASE_URL is
# primarily for the Postgres configuration.

DATABASE_BACKEND=postgres

# Use localhost when running code from the host/dev container. If another
# container links to Postgres, switch host to 'db'.
DATABASE_URL=postgresql+psycopg://app:app@localhost:5432/opponent_metrics

# ----------------------------------------------------------------------------
# Data Paths
# ----------------------------------------------------------------------------
DATA_ROOT=./data
STATSBOMB_DATA_PATH=./data/statsbomb
MODEL_ARTIFACTS_PATH=./data/artifacts

# ----------------------------------------------------------------------------
# API Configuration
# ----------------------------------------------------------------------------
API_HOST=0.0.0.0
API_PORT=8000

# ----------------------------------------------------------------------------
# Logging
# ----------------------------------------------------------------------------
LOG_LEVEL=INFO

